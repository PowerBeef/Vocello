{
  "schemaVersion": 1,
  "policyID": "retained-memory-v1",
  "metric": "withinModeRetainedPhysicalFootprintGrowth",
  "retentionThresholdFractionOfPhysicalMemory": 0.05,
  "variant": "speed",
  "length": "medium",
  "seed": 19790615,
  "modes": [
    "custom",
    "design",
    "clone"
  ],
  "repetitionsPerMode": 3,
  "expectedTakeCounts": {
    "macos": 11,
    "ios": 9
  },
  "unobservedGapBound": {
    "description": "Memory contract v2 timer health for every memory-qualified take: no gap between two consecutive samples of the process's one memory series may exceed max(targetIntervalMultiple x the sampler cadence, floorMS). The cadence is 500 ms on the 8 GB Mac and the iPhone, 250 ms on the 16 GB canonical M6 and 100 ms above, so the floor keeps one scheduler stall on a fast-cadence host from failing a take. Each take records the bound it met as samplerUnobservedGapLimitMS. Provisional: the first consented memory lane on the canonical M6 calibrates it, and a maintainer records that run here.",
    "targetIntervalMultiple": 2.0,
    "floorMS": 500,
    "status": "provisional",
    "calibrationRunID": null
  },
  "retainedMemoryV2": {
    "policyID": "retained-memory-v2",
    "metric": "withinModeRetainedMLXActiveGrowth",
    "description": "Reported beside retained-memory-v1 on the same run matrix. Each retained take publishes the MLX active and cache memory at its end (the snapshot after the routine post-generation cache clear, or after the stream on a tier that runs none) and the graphics-tagged footprint ledger at its end. Growth within a mode is the highest later retained take's MLX active memory minus the first's. It gates only once calibrated: until then every growthLimitMB is null and the record says uncalibrated.",
    "endOfTakeStages": [
      "after_generation_trim",
      "after_stream"
    ],
    "calibration": {
      "requirement": "One consented memory-qualification run per platform on its canonical host sets each mode's bound from that run's within-mode MLX growth noise; a maintainer records it here with the run ID.",
      "macos": {
        "status": "uncalibrated",
        "calibrationRunID": null,
        "growthLimitMBByMode": {
          "custom": null,
          "design": null,
          "clone": null
        }
      },
      "ios": {
        "status": "uncalibrated",
        "calibrationRunID": null,
        "growthLimitMBByMode": {
          "custom": null,
          "design": null,
          "clone": null
        }
      }
    }
  }
}
