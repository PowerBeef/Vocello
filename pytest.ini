[pytest]
testpaths = scripts/tests
python_files = test_*.py
addopts = -q -p no:cacheprovider --durations=15 --durations-min=1.0
markers =
    research: audio, delivery, prosody and device-analysis tooling
    darwin_only: needs the macOS host; skipped elsewhere
