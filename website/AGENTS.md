# Codex entry point — website

Read `../AGENTS.md`, `../CLAUDE.md` and `CLAUDE.md` in this directory. The latter owns website
commands, browser verification, architecture and product-claim sources. Read `PRODUCT.md` and
`DESIGN.md` here before visual or copy changes. Website-only work uses its Node/Playwright checks;
do not launch native builds, apps or device lanes for it. Use the shared handoff procedure in
`../docs/reference/development-workflow.md#claude-and-codex-handoffs`.
