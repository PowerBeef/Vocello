import Foundation

/// The planner lives in `Sources/SharedSupport/Services/WordTimingPlanner.swift`
/// since 2026-09-15 (both apps render karaoke transcripts); these names keep
/// the iOS call sites unchanged.
typealias IOSWordTimingPlanner = WordTimingPlanner
typealias IOSWordSpan = WordSpan
